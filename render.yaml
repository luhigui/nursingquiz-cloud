services:
  - type: web
    name: nursingquiz-cloud
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install --production
    startCommand: node server.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_VERSION
        value: 18
